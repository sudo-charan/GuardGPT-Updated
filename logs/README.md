# Logs directory

This directory stores runtime audit output and related generated logs.

Common generated files:
- `guardgpt_audit.jsonl`
- other operational logs created during local validation or debug runs

These artifacts are intentionally not committed to the repository unless specifically needed.
